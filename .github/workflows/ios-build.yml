# Intent: Build-only iOS workflow driven by pinned CI environment values defined solely in docs/ci/ci-readiness.md.

name: iOS Build

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  bootstrap:
    name: Read pinned CI environment
    runs-on: macos-14
    outputs:
      macos_runner: ${{ steps.read_env.outputs.macos_runner }}
      xcode_version: ${{ steps.read_env.outputs.xcode_version }}
      sim_device: ${{ steps.read_env.outputs.sim_device }}
      sim_os: ${{ steps.read_env.outputs.sim_os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Parse pinned CI environment
        id: read_env
        run: |
          set -euo pipefail
          source scripts/ci/readiness_env.sh

          if [[ -z "${CI_MACOS_RUNNER:-}" ]]; then
            echo "CI_MACOS_RUNNER is empty; ensure docs/ci/ci-readiness.md defines it." >&2
            exit 1
          fi

          echo "macos_runner=${CI_MACOS_RUNNER}" >> "$GITHUB_OUTPUT"
          echo "xcode_version=${CI_XCODE_VERSION}" >> "$GITHUB_OUTPUT"
          echo "sim_device=${CI_SIM_DEVICE}" >> "$GITHUB_OUTPUT"
          echo "sim_os=${CI_SIM_OS}" >> "$GITHUB_OUTPUT"

      - name: Guard pinned runner output
        run: |
          if [[ -z "${{ steps.read_env.outputs.macos_runner }}" ]]; then
            echo "Bootstrap output macos_runner is empty; verify docs/ci/ci-readiness.md" >&2
            exit 1
          fi

          echo "Resolved CI_MACOS_RUNNER from bootstrap: ${{ steps.read_env.outputs.macos_runner }}"

  build:
    name: Build with pinned environment
    needs: bootstrap
    runs-on: ${{ needs.bootstrap.outputs.macos_runner }}
    steps:
      - name: Guard resolved runner
        run: |
          set -euo pipefail
          echo "Using CI_MACOS_RUNNER from bootstrap: '${{ needs.bootstrap.outputs.macos_runner }}'"
          if [[ -z "${{ needs.bootstrap.outputs.macos_runner }}" ]]; then
            echo "build job cannot proceed without a pinned CI_MACOS_RUNNER from docs/ci/ci-readiness.md" >&2
            exit 1
          fi

      - name: Checkout
        uses: actions/checkout@v4

      - name: Parse pinned CI environment
        run: |
          set -euo pipefail
          source scripts/ci/readiness_env.sh

      - name: Print pinned values
        run: |
          echo "Pinned values from docs/ci/ci-readiness.md:" \
            "CI_MACOS_RUNNER=${CI_MACOS_RUNNER}" \
            "CI_XCODE_VERSION=${CI_XCODE_VERSION}" \
            "CI_SIM_DEVICE=${CI_SIM_DEVICE}" \
            "CI_SIM_OS=${CI_SIM_OS}"

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: ${{ needs.bootstrap.outputs.xcode_version }}

      - name: Show runner versions
        run: |
          sw_vers
          xcodebuild -version

      - name: Preflight
        run: scripts/ios/preflight.sh

      - name: Build
        run: scripts/ios/build.sh

      - name: Write diagnostics report
        run: |
          mkdir -p .ci
          {
            echo "CI_MACOS_RUNNER=${CI_MACOS_RUNNER}";
            echo "CI_XCODE_VERSION=${CI_XCODE_VERSION}";
            echo "CI_SIM_DEVICE=${CI_SIM_DEVICE}";
            echo "CI_SIM_OS=${CI_SIM_OS}";
            echo "";
            echo "sw_vers:";
            sw_vers;
            echo "";
            echo "xcodebuild -version:";
            xcodebuild -version;
          } > .ci/env-report.txt

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-ci-env-report
          path: .ci/env-report.txt
